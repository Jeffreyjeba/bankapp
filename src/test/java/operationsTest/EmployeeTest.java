package operationsTest;

import org.json.JSONObject;
import org.testng.annotations.DataProvider;
import org.testng.annotations.Test;

import netscape.javascript.JSObject;
import operations.Employee;

/*
 * public class EmployeeTest {
 * 
 * 
 * Employee employee= new Employee();
 * 
 * @Test(dataProvider = "userData") public void AddUser() {
 * employee.addUsers(null); }
 * 
 * @DataProvider(name="userData") protected JSONObject[][] userData(){ return
 * new JSONObject[][] { {new
 * JSONObject().put("","").put("","").put("","").put("","")},
 * 
 * } }
 * 
 * }
 */